/**
 * MQ 可靠性面试示例包。
 *
 * <p>本包用一个纯 JDK 的迷你 Broker/消费者仿真，串起 MQ 可靠性的三个核心问题：
 * 1. 至少一次语义下 ACK 丢失导致重复投递，消费端用“业务键 + Redis + DB 唯一索引”做幂等。
 * 2. 顺序性靠“业务键哈希路由到固定队列 + 同队列单线程串行消费”保证，代价是并行度。
 * 3. RocketMQ 事务消息的半消息、二阶段确认与本地事务回查机制（Kafka 原生不具备）。
 */
package com.advancedjava.interview.mq;
